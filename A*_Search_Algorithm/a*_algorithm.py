import utils

# pair = (i, j) for storing coordinates of cell => or maybe lets define a struct?
# pair_2 = (f,(i,j)) => maybe lets define struct? here f = g + h and (i,j) are the coordinates of cell
pair = (1,2)
pair_2 = (1,(1,2))
print(pair, pair_2)

def a_star_algorithm():
    return True